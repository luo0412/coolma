# 2026-04-13

> ⚠️ 此文件内容已过时，请参考 `OFFLINE_PLAN_V2.md` 和 `2026-04-17.md` 获取最新状态。

## 离线优先同步方案 — 本日改造

### 完成内容

**阶段1 全部完成（4个核心action + 笔记切换自动保存）：**

1. **`_updateNote` 修复 re-query bug** (`actions.js` ~572行)
   - `_pushToCloud` 里之前重新查询 `DatabaseClient.getNoteByDocGuid(docGuid)`，改为直接用 Step1 捕获的 `localNoteId`
   
2. **为 `DatabaseClient` 添加 `createGuidMapping` 方法** (`src/utils/DatabaseClient.js`)
   - `actions.js` 第 680 行调用 `DatabaseClient.createGuidMapping(localNoteId, result.guid, 'wiznote')`
   - 对齐主进程参数名：`cloudGuid` → `serverGuid`

3. **修复 `offline/deleteNote` double-delete bug** (`src/store/offline/index.js`)
   - `actions.js` 的 `deleteNote` 已经删了 SQLite，然后 `dispatch('offline/deleteNote', ...)`
   - `offline/deleteNote` 又调用 `DatabaseClient.deleteNote(noteId)` 导致二次删除失败
   - 改为：`offline/deleteNote` 只操作 Vuex 状态，数据库删除统一由 `actions.js` 处理

4. **Muya.vue 笔记切换自动保存** (`src/components/ui/editor/Muya.vue`)
   - `watch.currentNote` 中：切换前检测 `noteState === 'changed' || noteState === 'none'` 就 dispatch `updateNote`
   - 不 await，不阻塞新笔记加载，后台保存到 SQLite

### TODO.md 更新
- 标记阶段1全部完成
- 添加"已知Bug（已修复）"章节
- 更新文件清单状态

### 待进行
- 阶段2：SyncService 完善（pushToCloud 不动文件夹、定时同步、启动 sync）
- 阶段3：Header.vue 同步状态指示器
- 阶段3：NoteList.vue 冲突角标
- 阶段4：SettingsDialog 离线开关
